package io.hhplus.tdd.point.mok;

import io.hhplus.tdd.point.UserPoint;
import io.hhplus.tdd.serviceDTO.PointDTO;

public class UserPointRepository {

    public PointDTO selectById(PointDTO param) {
        return PointDTO.builder().build();
    }

    public PointDTO insertOrUpdate(PointDTO param) {
        return PointDTO.builder().build();
    }

}
